import React from "react";
import Layout from "../layouts/pages/layout";

const Calendario = () => {
  return (
    <Layout>
        {/*Este sera para el inicio del dashboard */}
      <h1>Bienvenido a mi página ERP Calendario</h1>
    </Layout>
  );
};

export default Calendario;